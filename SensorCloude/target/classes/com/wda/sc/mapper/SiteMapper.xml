<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wda.sc.mapper.SiteMapper">

	<select id="getList" resultType="com.wda.sc.domain.siteVO">
		select site_id,type_no,site_name,address,site_status,substr(start_date,0,14) as start_date from site
	</select>

	<select id="getSite" resultType="com.wda.sc.domain.siteVO">
		select * from site where site_id = #{site_id}
	</select>
	
	<select id="getAlarm_member" resultType="com.wda.sc.domain.alarm_memberVO">
		select name,tel from alarm_member where site_id = #{site_id}
	</select>
</mapper>